# MySQL 开发环境：启动前先执行 schema-mysql.sql
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fan_control?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  sql:
    init:
      mode: never
